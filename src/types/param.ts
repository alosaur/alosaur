export enum ParamType {
  Cookie = "cookie",
  Context = "context",
  Response = "response",
  Request = "request",
  Query = "query",
  QueryObj = "query-obj",
  RouteParam = "route-param",
  Body = "body",
}
